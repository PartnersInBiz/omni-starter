{
  "name": "sockets",
  "private": true,
  "type": "module",
  "devDependencies": {
    "nuxt": "^3.10.3"
  },
  "scripts": {
    "prepare": "nuxi prepare",
    "dev": "nuxi dev --open",
    "build": "nuxi build",
    "generate": "nuxi generate",
    "clean": "rimraf .nuxt .output node_modules"
  },
  "dependencies": {
    "@vueuse/core": "^10.9.0",
    "websocket-ts": "^2.1.5"
  }
}
